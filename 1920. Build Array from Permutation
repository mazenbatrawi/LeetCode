class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int> ans((int) nums.size());
        for(int i = 0; i < (int) nums.size(); i++){
            ans[i] = nums[nums[i]];
        }
        return ans;
    }
};
